import { useState } from "react";

interface useApiArguments {
  path: string,
}

const useApi = async ({
//   name,
//   params = {},
//   filters = {},
//   unwatchedFilters = {},
//   requestParams = {},
//   callback,
//   init = true,
//   afterCallback,
//   headers = {},
//   initialValue,
//   afterInit,
//   popup = true,
}: useApiArguments) => {
  try {
    
  } catch (e) {
    // console.error(e);
  }
};

export default useApi;
